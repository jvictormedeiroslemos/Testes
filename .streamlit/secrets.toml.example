# Arquivo de exemplo para .streamlit/secrets.toml
#
# INSTRUÇÕES:
# 1. Copie este arquivo e renomeie para: .streamlit/secrets.toml
# 2. Execute `python generate_password_hash.py` para gerar o hash da sua senha
# 3. Substitua o valor de PASSWORD_HASH pelo hash gerado
# 4. NUNCA compartilhe o arquivo secrets.toml — ele está no .gitignore
#
# No Streamlit Cloud:
# - Acesse: App Settings > Secrets
# - Cole as chaves abaixo diretamente no painel (sem criar o arquivo)

# Hash SHA-256 da sua senha de acesso ao sistema
# Gere com: python generate_password_hash.py
PASSWORD_HASH = "cole_aqui_o_hash_gerado"

# Chave da API Anthropic (opcional — pode ser informada no próprio app)
# Obtenha em: https://console.anthropic.com
ANTHROPIC_API_KEY = "sk-ant-sua-chave-aqui"
